"""Deterministic trust boundary — pure, network-free Python.

The LLM proposes; this package disposes. Nothing here may import the `llm`
package: the separation is what makes "would rather abstain than guess" a
property of the code, not a discipline that can erode.
"""
